management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    root: debug

spring:
  jpa:
    show-sql: true

simple-security:
  authentication-secret: secret
  enabled: false